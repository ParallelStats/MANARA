# Public canonical origin used only for metadata URLs. Do not place provider secrets in public variables.
MANARA_SITE_URL=http://localhost:3000

# Optional private Gemini server credential. Never rename this with a NEXT_PUBLIC_ prefix.
# Leave blank for MANARA's complete deterministic conversation path.
GEMINI_API_KEY=
